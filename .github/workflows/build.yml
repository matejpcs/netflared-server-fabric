name: Build Fabric 26.x

on:
  push:
    branches: ["26.x"]
  pull_request:
    branches: ["26.x"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: netflared-fabric-26-x
  cancel-in-progress: true

jobs:
  build:
    name: Fabric ${{ matrix.mc }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - mc: "26.1.1"
            loader: "0.19.2"
            api: "0.153.0+26.1.1"
            java: "25"
          - mc: "26.1.2"
            loader: "0.19.2"
            api: "0.153.1+26.1.2"
            java: "25"
          - mc: "26.2"
            loader: "0.19.3"
            api: "0.154.0+26.2"
            java: "25"
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "${{ matrix.java }}"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "9.7.1"
      - name: Build
        run: gradle clean build --no-daemon --stacktrace --refresh-dependencies -Pminecraft_version="${{ matrix.mc }}" -Ploader_version="${{ matrix.loader }}" -Pfabric_api_version="${{ matrix.api }}"
      - name: Verify JAR
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          jars=(build/libs/*.jar)
          test "${#jars[@]}" -gt 0
          printf '%s
' "${jars[@]}"
      - name: Upload JAR
        uses: actions/upload-artifact@v6
        with:
          name: netflared-fabric-26-x
          path: build/libs/*.jar
          if-no-files-found: error
