name: Build Fabric 1.20.x

on:
  push:
    branches: ["1.20.x"]
  pull_request:
    branches: ["1.20.x"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "9.5.1"
      - name: Build
        run: gradle clean build --no-daemon --stacktrace --refresh-dependencies -Pminecraft_version="1.20.6" -Ploader_version="0.15.11" -Pfabric_api_version="0.100.8+1.20.6"
      - name: Verify JAR
        run: test -n "$(find build/libs -maxdepth 1 -name '*.jar' -print -quit)"
      - uses: actions/upload-artifact@v6
        with:
          name: netflared-fabric-1.20.6
          path: build/libs/*.jar
          if-no-files-found: error
